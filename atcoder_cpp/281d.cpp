#include <iostream>
#include <string>
#include <unordered_map>
#include <vector>
using namespace std;

int main() {
  // input
  // int n, k, d;
  // cin >> n >> k >> d;

  // vector<vector<int>> dp(n, vector<int>(d, -1));
  // for (int i = 0; i < n; ++i) {
  //   int a;
  //   cin >> a;
  //   for (int j = 0; j < d; ++j) {
  //     if (i == 0) {}
  //     int diff = j - a;
  //     int moduloD;
  //     if (diff < 0) {
  //       moduloD = (d + diff) % d;
  //     } else {
  //       moduloD = diff;
  //     }

  //     dp[i][j] = max(dp[i - 1][j], dp[i - 1][moduloD]);
  //   }
  // }

  // cout << dp[n - 1][0] << endl;
  return 0;
}